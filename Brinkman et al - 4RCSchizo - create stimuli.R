library(rcicr)
base_face_files <- list('face'='MNES.jpg')
generateStimuli2IFC(base_face_files, n_trials = 900)

#for the RC schizo study, we only use the images with the extentio '*_ori.png'.
#see manuscripts for details of the experiment.
